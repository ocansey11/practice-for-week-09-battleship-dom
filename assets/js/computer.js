import Board from "./board";

export default class computerPlayer extends Board {
  constructor() {
    super(); // Call the constructor of the base class
    // Add any additional setup for HumanPlayer
  }
}
